const gameWidth = 820.0;
const gameHeight = 1600.0;
const ballWidth = gameWidth * 0.02;
const batWidth = gameWidth * 0.2; // Add from here...
const batHeight = ballWidth * 2;
const batStep = gameWidth * 0.05;
